---
const { wide = false } = Astro.props;
---
<div class={`mx-auto flex min-h-dvh ${wide ? "max-w-3xl" : "max-w-xl"} items-center justify-center px-5`}>
  <div class="my-4 w-full">
    <slot />
  </div>
</div>
